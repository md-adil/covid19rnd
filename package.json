{
	"scripts": {
		"start": "parcel src/index.html",
		"build": "NODE_ENV=development parcel build src/index.html --out-dir .",
		"clean": "rm *.html *.css *.js *.map"
	},
	"devDependencies": {
		"cssnano": "^4.1.10"
	}
}
